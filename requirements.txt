# PLM HF Space - uses Inference API, no heavy local models
gradio>=5.0
huggingface-hub>=0.23
requests>=2.31
Pillow>=10.0.0
ffmpeg-python>=0.2.0

# Note: ffmpeg system binary is installed via packages.txt (apt)
