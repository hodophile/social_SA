# VideoPrism HF Space - runs google/videoprism-base-f16r288 locally
gradio>=5.0.0
torch>=2.1
transformers>=4.45
accelerate>=0.30
sentence-transformers>=2.5.0
huggingface-hub>=0.28.1
Pillow>=10.0.0
numpy>=1.24

# Note: ffmpeg system binary is installed via Dockerfile (apt)
